import React from 'react';

const NotFoundPage = () => (
  <div>
    <h1>Not Found</h1>
    <p>The page you were looking for can't be found. Go back, maybe?</p>
  </div>
);

export default NotFoundPage;
